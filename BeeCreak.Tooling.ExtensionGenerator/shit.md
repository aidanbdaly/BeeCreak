Things this takes care of (its a piece of shit)

Loading other assets when the name of a parameter is the same name as an asset (appending Array to the name will treat an array of strings as an array of that asset)

expressing 2d arrays properly in writers

inheriting list of asset types from another schema.json